import { index, integer, real, sqliteTable, text } from 'drizzle-orm/sqlite-core';

export const users = sqliteTable('users', { id: text('id').primaryKey(), name: text('name').notNull(), phone: text('phone').notNull(), email: text('email'), role: text('role', { enum: ['admin', 'agent', 'customer'] }).notNull(), assignedAgentId: text('assigned_agent_id'), createdAt: integer('created_at', { mode: 'timestamp' }).notNull() });
export const loans = sqliteTable('loans', { id: text('id').primaryKey(), customerId: text('customer_id').notNull(), principal: integer('principal').notNull(), balance: integer('balance').notNull(), interestType: text('interest_type', { enum: ['fixed', 'floating'] }).notNull(), interestRate: real('interest_rate').notNull(), repaymentFrequency: text('repayment_frequency', { enum: ['daily', 'weekly', 'monthly', 'yearly'] }).notNull(), givenDate: text('given_date').notNull(), nextDueDate: text('next_due_date').notNull(), securityType: text('security_type', { enum: ['asset', 'surety'] }).notNull(), securityFileKey: text('security_file_key'), remarks: text('remarks'), status: text('status').notNull().default('active') }, (table) => [index('idx_loans_customer_id').on(table.customerId), index('idx_loans_next_due_date').on(table.nextDueDate)]);
export const collections = sqliteTable('collections', { id: text('id').primaryKey(), loanId: text('loan_id').notNull(), agentId: text('agent_id').notNull(), amount: integer('amount').notNull(), method: text('method', { enum: ['cash', 'upi', 'bank'] }).notNull(), proofFileKey: text('proof_file_key'), remarks: text('remarks'), collectedAt: integer('collected_at', { mode: 'timestamp' }).notNull() }, (table) => [index('idx_collections_agent_date').on(table.agentId, table.collectedAt), index('idx_collections_loan_id').on(table.loanId)]);
export const topups = sqliteTable('topups', { id: text('id').primaryKey(), loanId: text('loan_id').notNull(), amount: integer('amount').notNull(), interestType: text('interest_type', { enum: ['fixed', 'floating'] }).notNull(), securityFileKey: text('security_file_key'), remarks: text('remarks'), status: text('status', { enum: ['pending', 'approved', 'declined'] }).notNull().default('pending'), requestedAt: integer('requested_at', { mode: 'timestamp' }).notNull() });
export const dueExceptions = sqliteTable('due_exceptions', { id: text('id').primaryKey(), loanId: text('loan_id').notNull(), originalDueDate: text('original_due_date').notNull(), revisedDueDate: text('revised_due_date').notNull(), remarks: text('remarks').notNull(), approvedBy: text('approved_by') });
